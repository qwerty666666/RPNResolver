package expression;


/**
 * Superinterface for all operands.
 * The interface only describes which classes can be considered as operand in Expression.
 */
public interface Operand<T> extends Token {
}
